import ReactDOM from 'react-dom';
import React from 'react';
import FormContainer from './component/container/FormContainer.jsx';

const wrapper = document.getElementById("app");

ReactDOM.render(<FormContainer />, wrapper);